name: wheelchair_app
description: SmartChair — voice-controlled wheelchair with live mapping.
publish_to: "none"
version: 3.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  speech_to_text: ^7.0.0
  mobile_scanner: ^5.0.0
  nfc_manager: ^3.5.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
